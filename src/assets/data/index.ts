import * as peopleData from './people.json';

export { peopleData };
//todo usage "resolveJsonModule": true in tsconfig can lead to node.js incompatability when will add backend with node.js;
// large json will impact to app load time
